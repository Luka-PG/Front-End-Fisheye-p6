
function galleryTemplate () {

    const { title, image ,price, likes} = data;

    function galleryCard() {

        const gallery = document.querySelector('.media-section')

       // const sorting = document.createElement('div')
        if ( data. ('video')) {
            const photograph = document.createElement('img')
            photograph.setAttribute("src", image)
            photograph.setAttribute("alt", title)
    }}
    return
}